name: node_inspector_app
description: A standalone node import, inspection, rename, and Karing export application.
publish_to: "none"
version: 0.4.0+4

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  crypto: ^3.0.6
  file_selector: ^1.0.3
  flutter:
    sdk: flutter
  yaml: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/core/
